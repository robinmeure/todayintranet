define([], function () {
  return {
    PropertyPaneDescription:
      'Page settings for the Today dashboard. Everyone who visits the page arranges their own widgets from the page itself.',
    BasicGroupName: 'Dashboard',
    TitleFieldLabel: 'Dashboard title',
    StorageGroupName: 'Saved layouts',
    DashboardIdFieldLabel: 'Dashboard id',
    DashboardIdFieldDescription:
      'Scopes the layouts people save on this site. Leave it as "default" unless this site has a second Today dashboard that needs its own arrangements. Changing it starts everyone from the default layout again.'
  };
});
